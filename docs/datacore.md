# Datacore — binary format reference

> **Canonical documentation lives in the Obsidian vault at `D:\Obsidian\Star Citizen\Game Files\Datacore.md`.**
>
> That version is cross-checked against both [Svarog](https://github.com/19h/Svarog.git) and [StarBreaker](https://github.com/nickkuk/StarBreaker) Rust source, includes corrections (120-byte header, `struct_size` field renames, v8+ record variant), and is the authoritative reference.
>
> This file previously held a full copy of the format spec. It was replaced with this redirect on 2026-04-16 to avoid maintaining two copies that drift out of sync.
>
> See also: `Datacore Enums.md` in the vault for `DataTypes` and `ConversionTypes` lookup tables.
