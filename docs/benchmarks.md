# sc-holotable — benchmarks

> Compile-time and runtime benchmarks for `sc-extract` +
> `sc-extract-generated`. Measurements are **run manually** via
> [`tools/bench/bench.ps1`](../tools/bench/bench.ps1); see its
> [README](../tools/bench/README.md) for usage.
>
> The **Latest results** section further down is auto-generated by the
> script — don't hand-edit it. Everything else on this page (methodology,
> history, interpretation) is hand-written and preserved across runs.

## Running the benchmarks

```powershell
# From the workspace root. Full suite, ~30–60 minutes.
# -KillRa stops rust-analyzer before measuring (required for clean numbers).
.\tools\bench\bench.ps1 -Mode all -KillRa
```

The script writes:

- **`target/bench-results.json`** — structured raw data, full environment snapshot, per-run timestamp.
- **This file's *Latest results* section** — auto-replaced in place via markdown markers.

When you run the script, the **Latest results** section below gets
overwritten with the new numbers. If you want to preserve the previous
run's numbers as a trend datapoint, copy them into the **History**
section at the bottom *before* re-running — the script does not merge
into history.

## What the modes measure

| Mode | Measures | Cost |
|---|---|---|
| `check` | Cold `cargo check -p sc-extract` for each feature set | ~10 min |
| `build` | Cold `cargo build -p sc-extract --profile dev-opt` for each feature set | ~30 min |
| `runtime` | `parse_real_p4k` against real `Data.p4k`, with + without reference graph | ~5 min |
| `all` | Everything above | ~45 min |

See [`tools/bench/README.md`](../tools/bench/README.md) for the full
definition of each mode (what gets cleaned, what gets timed, how feature
sets are routed).

## Memory sampling

The script samples `WorkingSet64` across all `cargo`/`rustc`/`link`/`lld-link`/`parse_real_p4k`
processes every 500 ms and reports the peak total. This is a sum across
concurrent processes (rustc often runs multiple codegen units in
parallel) so it reflects the true system-wide RAM high-water mark for
the build.

**Caveats to keep in mind when reading Peak RAM:**

- Sampling has a 500 ms granularity. Sub-500 ms peaks during fast link
  phases are sometimes missed. Long-running rustc phases
  (monomorphization, typecheck) plateau for many seconds, so in
  practice this matters only at the very end of a build.
- The script samples processes by name, not by parent-child tree. Under
  `-KillRa` this is accurate because rust-analyzer has been stopped.
  Under `-IgnoreRa` the sample includes whatever rust-analyzer is
  running concurrently, which inflates the reported peak. That's
  intentional — it matches the "day-to-day with IDE" reading.
- Working set is committed RSS, not virtual size. It's the number that
  matches Task Manager's memory column and what matters for OOM risk.

## Findings from 2026-04-15

Three things surfaced from the first clean (RA-paused) benchmark run
that are worth investigating before starting on `sc-weapons`:

### Full dev-opt build regression

Pre-polymorphism (v1 generator, 1,935 types): **20m 56s** cold.
Post-polymorphism (v2 generator, 6,234 types): **3h 25m 58s** cold.

That's a ~10× regression. `cargo check --features full` at 4m 5s
scales in line with the 3.2× type-count increase, so the typecheck
path is roughly proportional. The `dev-opt` build path is not — which
points at codegen, not typecheck, as the dominant cost. Likely
suspects: serde-derive monomorphization of 6k structs + ~1.2k enums,
or `sc-extract-generated`'s `opt-level = 1 + codegen-units = 256`
interacting badly with the new type count (256 codegen units × many
small units might serialize poorly on an 8-core box).

Action items worth trying, rough cost/benefit:

- **`cargo build --timings -p sc-extract --profile dev-opt --features full`**
  — cheap diagnostic. Gives a per-crate + per-codegen-unit breakdown.
  Do this first.
- **Drop codegen-units to 16 or 64 temporarily** and re-measure.
  `codegen-units = 256` was picked for the pre-polymorphism type
  count and may be exactly wrong at 6k.
- **Disable serde derives on pool structs** to see if that's the bulk.
  Hacky — leave for last.

### `entities-scitem-ships` hub leaf: 34m

Single leaf feature, but the dev-opt cold build takes 34 minutes and
peaks at 10.5 GB. `ammoparams` at the same profile is 3m 58s / 2.4 GB.
Something about the ships closure is pathological — probably pulling
in most of `multi_feature` because ship records reference so much of
the DCB. This is the "hub leaf problem" flagged in
`docs/feature-gating-v2.md` §"Hub-leaf problem"; it's now measured
and worth documenting as a warning for anyone trying to depend on
this feature from a domain crate. Narrow consumers of ship data
should use more specific sub-features once we split this hub.

### Full build peak RAM at 25.7 GB

On a 32 GB machine this leaves ~6 GB headroom for the OS + browser +
VSCode. Any dev on 16 GB will OOM during `cargo build --features full`.
Worth noting in `CLAUDE.md` as a hard requirement. `cargo check` full
at 11.7 GB is much more manageable and still useful for iteration.

## Interpretation

- **Default cold check should be significantly faster than full cold check.**
  The v2 feature-gating design puts most types behind per-type
  `cfg(any(...))` unions, so with no features enabled rustc strips
  most of `multi_feature/` during parsing. If default approaches full,
  the classifier has drifted toward schema-static and wants
  investigation.
- **`dormant` should be marginally slower than `full`.** Adds 809
  types whose references are already in-scope via the `full` forward.
  If `dormant` is wildly slower than `full`, the dormant set grew
  unexpectedly — check the generator's dormancy classification.
- **Runtime parse under default should return 0 records.** Not a bug
  — no record extractor dispatch is compiled when no leaf feature is
  enabled.
- **Runtime parse under `--features full` is the realistic consumer
  number.** This is what a snapshot-building CI job or a domain crate
  like sc-weapons will see.
- **Snapshot size with graph ≫ without.** The reference graph
  (~1.4M edges) dominates the msgpack payload for the default config.
  With graph off, snapshots stay in the single-digit-MB range.
- **Peak RAM during `dev-opt` build of `full` is the canonical
  "how much memory does rustc need here" number.** If this ever
  exceeds ~12 GB, domain-crate authors on 16 GB machines will start
  hitting paging. Current target: stay under 10 GB.

<!-- BENCH:RESULTS-START -->
<!-- This block is auto-generated by tools/bench/bench.ps1. Edit the script, not the markdown, if you want to change the format. -->

## Latest results

*Last run: 2026-04-15T22:26:21.6226467+02:00. Mode: `all`. rust-analyzer: not running.*

Rows marked `(skipped)` or `(aborted)` were cut short by the operator. Re-run with `-Features <name> -Mode <mode>` to fill them in.

### Environment

| Field | Value |
|---|---|
| os | Microsoft Windows 11 Pro |
| cpu | AMD Ryzen 7 5700X3D 8-Core Processor            |
| ram | 32 GB |
| rustc | rustc 1.94.1 (e408947bf 2026-03-25) |
| cargo | cargo 1.94.1 (29ea6fb6a 2026-03-24) |

### Cold `cargo check`

| Features | Cold check | Peak RAM |
|---|---:|---:|
| `default` | 9.17s | 555 MB |
| `ammoparams` | 9.62s | 600 MB |
| `entities-scitem-ships` | 15.45s | 1,009 MB |
| `full` | 37.66s | 2.54 GB |

### Cold `cargo build --profile dev-opt`

| Features | Cold build | Peak RAM |
|---|---:|---:|
| `default` | 29.29s | 1.46 GB |
| `ammoparams` | 29.94s | 1.43 GB |
| `entities-scitem-ships` | 1m 8.2s | 1.71 GB |
| `full` | 2m 53.9s | 4.41 GB |

### Runtime `parse_real_p4k` — `DatacoreConfig::standard()`

| Features | Records | Locale | Display names | Parse | Snapshot | Save | Load | Peak RAM |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `default` | 0 | 87,626 | 24,017 | 1.97s | 35.16 MB | 5.44s | 2.35s | 2.59 GB |
| `ammoparams` | 46,255 | 87,626 | 24,017 | 7.44s | 35.16 MB | 5.45s | 2.35s | 3.17 GB |
| `entities-scitem-ships` | 66,839 | 87,626 | 24,017 | 35.59s | 35.16 MB | 5.65s | 2.44s | 4.30 GB |
| `full` | 111,928 | 87,626 | 24,017 | 36.74s | 35.16 MB | 6.04s | 2.58s | 4.58 GB |

### Runtime `parse_real_p4k` — `DatacoreConfig::all()` (reference graph on)

| Features | Records | Graph edges | Parse | Snapshot | Save | Load | Peak RAM |
|---|---:|---:|---:|---:|---:|---:|---:|
| `default` | 0 | 1,426,550 | 9.01s | 35.16 MB | 5.43s | 2.35s | 2.73 GB |
| `ammoparams` | 46,255 | 1,426,550 | 14.28s | 35.16 MB | 5.46s | 2.46s | 3.27 GB |
| `entities-scitem-ships` | 66,839 | 1,426,550 | 42.34s | 35.16 MB | 5.69s | 2.51s | 4.37 GB |
| `full` | 111,928 | 1,426,550 | 45.37s | 35.16 MB | 5.91s | 2.55s | 4.65 GB |

<!-- BENCH:RESULTS-END -->

## History

Previous benchmark runs for trend tracking. **Before re-running the
script, copy the tables from the "Latest results" section above into
a new dated subsection here** — the script's auto-update does not
merge old results.

### Pre-2026-04-15 (stale, retained for context)

These numbers come from the feature-gating v1 generator output
(~1,935 emitted types, pre-polymorphism). They are **not directly
comparable** to post-v2 numbers — the emitted type count is ~3.2×
larger now, compile times are dominated by different terms, and the
`core` classification rules changed.

Compile (cold `cargo check`, SC 4.7 branch, 2026-04-12):

| Features | Cold check |
|---|---:|
| Core only (default) | 26s |
| Core + ammoparams | 28s |
| Full (1,935 types) | 1m 41s |

Compile (cold `dev-opt` build, SC 4.7 branch, 2026-04-12):

| Features | Cold build |
|---|---:|
| Core only | 4m 19s |
| Core + ships | 4m 38s |
| Full | 20m 56s |

Runtime (parse_real_p4k, 2026-04-12, 1,935-type build):

| Features | Records | Parse | Snapshot | Load |
|---|---:|---:|---:|---:|
| Core only | 60,669 | 17.0s | 12.35 MB | 1.35s |
| Core + ships | 60,836 | 16.4s | 12.42 MB | 1.34s |
| Full | 111,928 | 18.9s | 16.79 MB | 2.33s |

With reference graph (`--all`, full features):

| Metric | Value |
|---|---|
| Parse time | ~25s |
| Graph build | ~7.4s |
| Graph edges | 1,427,349 |
| Snapshot with graph | 34.88 MB |

### 2026-04-15 21:04 (clean, post-snapshot-v5, pre-derive-drop)

First clean (RA-paused) run after the snapshot v5 byte-bundle rework
brought full `dev-opt` build from ~3h 26m down to 7m 10s. Generated
structs still carried `#[derive(Debug, Clone, Serialize, Deserialize)]`
and every field still had `#[serde(default)]`; enums carried the same
serde derives plus a manual `Default` impl. The next run in this
history should show the impact of dropping those.

Environment:

| Field | Value |
|---|---|
| os | Microsoft Windows 11 Pro |
| cpu | AMD Ryzen 7 5700X3D 8-Core Processor |
| ram | 32 GB |
| rustc | rustc 1.94.1 (e408947bf 2026-03-25) |
| cargo | cargo 1.94.1 (29ea6fb6a 2026-03-24) |

Cold `cargo check`:

| Features | Cold check | Peak RAM |
|---|---:|---:|
| `default` | 36.45s | 1.77 GB |
| `ammoparams` | 42.11s | 2.07 GB |
| `entities-scitem-ships` | 1m 40.2s | 5.09 GB |
| `full` | 4m 19.1s | 11.70 GB |
| `dormant` | 4m 6.8s | 11.71 GB |

Cold `cargo build --profile dev-opt`:

| Features | Cold build | Peak RAM |
|---|---:|---:|
| `default` | 1m 3.2s | 1.96 GB |
| `ammoparams` | 1m 7.5s | 2.38 GB |
| `entities-scitem-ships` | 2m 48.5s | 5.56 GB |
| `full` | 7m 10.1s | 13.63 GB |
| `dormant` | 7m 3.2s | 13.60 GB |

Runtime `parse_real_p4k` — `DatacoreConfig::standard()`:

| Features | Records | Locale | Display names | Parse | Snapshot | Save | Load | Peak RAM |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `default` | 0 | 87,626 | 24,017 | 1.93s | 35.16 MB | 5.58s | 2.63s | 2.03 GB |
| `full` | 111,928 | 87,626 | 24,017 | 35.55s | 35.16 MB | 5.71s | 2.50s | 4.57 GB |

Runtime `parse_real_p4k` — `DatacoreConfig::all()` (reference graph on):

| Features | Records | Graph edges | Parse | Snapshot | Save | Load | Peak RAM |
|---|---:|---:|---:|---:|---:|---:|---:|
| `default` | 0 | 1,426,550 | 8.83s | 35.16 MB | 5.54s | 2.61s | 2.72 GB |
| `full` | 111,928 | 1,426,550 | 42.55s | 35.16 MB | 5.68s | 2.62s | 4.65 GB |

### 2026-04-15 (partial, contaminated by rust-analyzer)

Quick-and-dirty numbers captured during the docs refresh on 2026-04-15.
rust-analyzer was running throughout with `cargo.features = "all"`,
so these are day-to-day "IDE open in the background" numbers, not
clean-room measurements. Memory was not sampled. Kept for context;
replace the "Latest results" section with a clean run whenever the
next `bench.ps1 -KillRa` happens.

Cold `cargo check`:

| Features | Cold check |
|---|---:|
| default | 34.24s |
| `--features ammoparams` | 38.98s |
| `--features entities-scitem-ships` | 1m 33s |
| `--features full` | 3m 57s |
| `--features dormant` | 3m 57s |

Cold `dev-opt` build (default features, full clean including deps):
**2m 29s**.

Runtime (default features, dev-opt example build):

| Config | Records | Parse | Graph edges | Snapshot | Save | Load |
|---|---:|---:|---:|---:|---:|---:|
| `DatacoreConfig::standard()` | 0 | 1.97s | 0 | 5.26 MB | 0.28s | 0.19s |
| `DatacoreConfig::all()` (graph on) | 0 | 10.66s | 1,426,550 | 23.09 MB | 2.46s | 3.53s |

**Observations from this partial run, worth verifying with a clean run:**

- Default cold check dropped from the 4m 15s baseline in commit
  `bc9f899` to 34s — strong evidence that the core/multi_feature split
  is working and rustc is stripping `multi_feature/` effectively when
  no leaf feature is enabled.
- Display names jumped from 0 (stale) to 24,017. The locale path that
  was flagged as broken is no longer broken — either `global.ini`
  moved back to a discoverable path or the LocaleKey work fixed the
  lookup incidentally. Close out that "Still open" item in `status.md`
  after the next clean run confirms.
- Reference graph edges dropped marginally from 1,427,349 to 1,426,550
  (−0.06%). Probably incidental patch-to-patch drift; not worth
  investigating unless it moves again.
