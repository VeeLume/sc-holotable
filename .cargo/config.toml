# Workspace-level Cargo configuration.
#
# Clean slate: no overrides. Previous settings (RUST_MIN_STACK bump,
# Windows linker stack bump) were workarounds for serde-derive
# compilation costs and rmp_serde runtime stack costs on generated
# types. With those derives gone, the defaults should suffice.
